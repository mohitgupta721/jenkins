pipeline {
   agent any
   environment {
       color = "blue"
   }
   stages {
       stage('first') {
            agent { label 'master' }
            steps {
               print ${env.DEVELOPER_NAME}
            }
        }
    }
}
