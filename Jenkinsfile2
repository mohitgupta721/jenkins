pipeline {
   agent any
   environment {
       color = "blue"
   }
   stages {
       stage('first') {
            agent { label 'master' }
            steps {
               echo ${env.DEVELOPER_NAME}
            }
        }
    }
}
